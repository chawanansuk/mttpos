# Medee POS

ระบบขายหน้าร้าน (POS) + หลังบ้าน สำหรับ **ร้านมีดีทวีคูณ สาขาสำเพ็ง**
สร้างตามสเปก `POS-Clone-Spec.md` — ภาษาไทยเป็นหลัก ใช้งานได้ทั้งบนแท็บเล็ต มือถือ และเดสก์ท็อป

```
┌─────────────────┐   ┌──────────────────┐
│  POS  /pos      │   │  หลังบ้าน /admin │   ← Next.js 15 (apps/web)
│  PWA · offline  │   │  รายงาน · จัดการ │
└────────┬────────┘   └────────┬─────────┘
         │  REST + WebSocket   │
         └──────────┬──────────┘
              apps/api (Fastify)
                     │
       packages/domain (สูตรคำนวณ) · packages/db (Prisma)
                     │
              PostgreSQL 15+
```

---

## เว็บที่ใช้งานอยู่

**https://medee-pos.vercel.app** — deploy จริงบน Vercel (ฟังก์ชันอยู่ที่ Singapore) + Supabase Postgres

เข้าสู่ระบบด้วยบัญชีในตารางด้านล่าง หรือกด "เข้าหน้าขาย (POS)" เพื่อใช้เครื่องขาย

> ⚠️ ลิงก์นี้เปิดสาธารณะและใช้รหัสผ่านชุดเดียวกับที่เขียนไว้ใน repo
> ใครที่ได้ URL ไปจะเข้าดูข้อมูลยอดขายทั้งหมดได้ — เปลี่ยนรหัสก่อนใช้กับข้อมูลจริงของร้าน

---

## เริ่มใช้งาน

ต้องมี **Node 20+**, **pnpm 10+** และ **PostgreSQL 15+**

```bash
pnpm install

# ตั้งค่าการเชื่อมต่อฐานข้อมูล
cp .env.example .env          # แล้วแก้ DATABASE_URL ให้ตรงกับเครื่องคุณ
cp .env packages/db/.env
cp .env apps/api/.env

pnpm db:generate              # สร้าง Prisma client
pnpm db:push                  # สร้างตารางในฐานข้อมูล
pnpm db:seed                  # ใส่ข้อมูลจริงของร้าน

pnpm dev                      # รัน API (:4000) และเว็บ (:3000) พร้อมกัน
```

เปิด <http://localhost:3000>

| ใช้เข้า | บัญชี | รหัสผ่าน |
|---|---|---|
| หลังบ้าน (เจ้าของ) | `medeetaweekoon.official@gmail.com` | `Medee@2026` |
| หลังบ้าน (ผู้จัดการ สิทธิ์จำกัด) | `medeesystem1@gmail.com` | `Medee@2026` |
| หน้าขาย — PIN พนักงาน | `Admin` = `0000` · `cashier1` = `1111` | |

> ⚠️ รหัสผ่านและ PIN ข้างต้นเป็นค่าตั้งต้นสำหรับทดลองใช้ **เปลี่ยนก่อนใช้งานจริง**

### รันด้วย Docker

```bash
docker compose up -d          # postgres + api + web
docker compose exec api pnpm db:push && docker compose exec api pnpm db:seed
```

### Deploy บน Vercel

ระบบรันได้สองแบบจากโค้ดชุดเดียวกัน ต่างกันแค่ว่าตั้ง `API_URL` ไว้หรือไม่:

| | เว็บ | API |
|---|---|---|
| **สองโปรเซส** (dev · Docker · VPS) | `apps/web` พอร์ต 3000 | `apps/api` พอร์ต 4000 — Next rewrite `/api/v1/*` ไปให้ |
| **โปรเซสเดียว** (Vercel) | `apps/web` | route handler เรียก `buildApp()` ของ Fastify ผ่าน `app.inject()` ในโปรเซสเดียวกัน |

ตั้งค่าโปรเจกต์บน Vercel:

- **Root Directory** — `apps/web`
- **Environment Variables** — ตั้งแค่สามตัวนี้ และ **ห้ามตั้ง** `API_URL` หรือ `NEXT_PUBLIC_API_URL`
  (ถ้าตั้ง เว็บจะพยายาม proxy ออกไปข้างนอกแทนที่จะเสิร์ฟ API เอง)

  ```
  DATABASE_URL         postgresql://...   ← ควรเป็น connection pooler
  JWT_SECRET           สตริงสุ่มยาว ๆ
  JWT_REFRESH_SECRET   สตริงสุ่มยาว ๆ อีกอัน
  ```

- **ใส่ข้อมูลตั้งต้น** — ถ้าเครื่องคุณต่อฐานข้อมูลได้ ใช้
  `DATABASE_URL=<ของ production> pnpm db:push && pnpm db:seed`

  ถ้าต่อไม่ได้ (เช่นอยู่หลังไฟร์วอลล์ที่เปิดแค่พอร์ต 443) ให้ยืมขั้นตอน build ของ Vercel ทำแทน
  โดยตั้งตัวแปรเพิ่มสองตัวแล้ว deploy ใหม่หนึ่งครั้ง:

  ```
  DIRECT_DATABASE_URL   การต่อแบบ session/ตรง (Supabase พอร์ต 5432) — ใช้สร้างตาราง
  SEED_DATABASE         yes-wipe-and-seed
  ```

  `scripts/bootstrap-db.mjs` จะรัน `prisma db push --force-reset` แล้ว seed ให้ตอน build
  **ลบ `SEED_DATABASE` ออกทันทีที่เสร็จ** ไม่งั้น deploy ครั้งถัดไปจะล้างข้อมูลจริงทิ้ง

> Supabase ต้องใช้สอง URL: พอร์ต **6543** (transaction pooler ต่อท้าย `?pgbouncer=true&connection_limit=1`)
> เป็น `DATABASE_URL` สำหรับตอนรัน และพอร์ต **5432** (session) เป็น `DIRECT_DATABASE_URL` สำหรับสร้างตาราง
> เพราะ PgBouncer โหมด transaction ใช้ prepared statement กับ advisory lock ที่ Prisma ต้องการไม่ได้

ข้อจำกัดของโหมด serverless:

- **ไม่มี WebSocket** — ฟังก์ชันไม่ได้อยู่ค้างเพื่อถือ connection หน้ารายงานสรุปกับบิลที่เปิดอยู่
  จะเปลี่ยนไปดึงข้อมูลซ้ำตามรอบ (30 และ 15 วินาที) โดยอัตโนมัติเมื่อพบว่าต่อ `/ws` ไม่ได้
- **rate limit ปิดอยู่** — `@fastify/rate-limit` นับในหน่วยความจำของแต่ละอินสแตนซ์ จึงไม่มีความหมายบน serverless
  ถ้าต้องการจริงให้ใช้ Vercel Firewall หรือย้ายตัวนับไปไว้ที่ Redis
- **ต้องใช้ connection pooler** — serverless เปิดหลายอินสแตนซ์พร้อมกัน ถ้าต่อ Postgres ตรงจะกิน connection จนเต็ม

---

## โครงสร้างโปรเจกต์

| แพ็กเกจ | หน้าที่ |
|---|---|
| `packages/domain` | **สูตรคำนวณทั้งหมด** — ยอดบิล · ส่วนลด · VAT · ปัดเศษ · step price · ราคาช่องทาง · สต็อก · ต้นทุนเฉลี่ย · รอบเงินสด · คะแนนสมาชิก · เลขเอกสาร · วันขาย · ตารางสิทธิ์ · PromptPay QR (ไม่พึ่งพา framework ใด ๆ และมี unit test ครบ) |
| `packages/db` | Prisma schema ตามหัวข้อ 4 + seed ข้อมูลจริงตามหัวข้อ 10 |
| `apps/api` | Fastify — REST `/api/v1/*` + WebSocket `/ws` · export `buildApp()` ให้ `apps/web` ยกไปรันในโปรเซสเดียวกันได้ |
| `apps/web` | Next.js — หน้าขาย `/pos` (PWA) และหลังบ้าน `/admin` |

**สูตรคำนวณอยู่ที่เดียว**: ทั้งเครื่องขาย (คำนวณสดในเครื่องแม้ออฟไลน์) และ API (คำนวณตอนบันทึกบิล) เรียก `calculateBill` ตัวเดียวกัน ยอดจึงตรงกันเสมอ

---

## คำสั่งที่ใช้บ่อย

```bash
pnpm dev             # รันทุกแอปพร้อมกัน
pnpm dev:api         # รันเฉพาะ API
pnpm dev:web         # รันเฉพาะเว็บ
pnpm test            # unit test สูตรคำนวณ + การแฮชรหัสผ่าน (149 เทสต์)
pnpm typecheck       # ตรวจ TypeScript ทุกแพ็กเกจ
pnpm db:seed         # ใส่ข้อมูลใหม่ (ล้างของเดิมก่อน)
pnpm db:reset        # ล้างตาราง + push schema + seed
pnpm db:studio       # เปิด Prisma Studio
node scripts/acceptance.mjs   # รัน acceptance checklist (ต้อง seed ใหม่ก่อน)
```

---

## สิ่งที่ระบบทำได้

### หน้าขาย (`/pos`)
ตั้งค่าเครื่องขาย · ล็อกอินด้วย PIN · เปิด/ปิดรอบเงินสด · นำเงินเข้า-ออก ·
กริดสินค้าตามกลุ่ม + แท็บสินค้า-นิยม · ยิงบาร์โค้ด (ยิงซ้ำ = เพิ่มจำนวน) ·
เลือกขนาดบรรจุ · ตัวเลือกเสริมแบบบังคับ/เลือกได้ · ราคาด่วน · step price ·
ราคาขายส่งสมาชิก · ส่วนลดรายการและท้ายบิล · ชำระเงินสด/พร้อมเพย์/หลายช่องทาง ·
พิมพ์ใบเสร็จ · พักบิลและเรียกกลับ · ยกเลิกบิลด้วย PIN · เพิ่มสินค้าแบบเร็ว ·
**ขายได้ขณะออฟไลน์** แล้วซิงค์อัตโนมัติเมื่อกลับมาออนไลน์

### หลังบ้าน (`/admin`)
รายงานสรุปพร้อมกราฟและเรียลไทม์ · ประวัติการขายแบบขยายดูรายการได้ ·
บิลที่เปิดอยู่ · **รายงาน 25 หน้า** (ขาย · คลัง · ภาษี · พนักงาน · ลูกค้า) ส่งออก xlsx/csv ได้ทุกหน้า ·
จัดการสินค้า 5 แท็บ · กลุ่มสินค้า/หน่วย/ตัวเลือก/กลุ่มตัวเลือก · ช่องทางการขาย + GP ·
จัดลำดับเมนูหน้าขาย · นำเข้าสินค้าจาก CSV · เอกสารคลัง (รับเข้า/จ่ายออก/ปรับปรุง/ตรวจนับ) ·
โอนระหว่างสาขา · บัตรสินค้า · สมาชิก + คะแนน + บันทึกของร้าน ·
ตั้งค่าร้าน/ใบเสร็จ (พร้อม live preview)/การชำระเงิน/พนักงาน/**ตารางสิทธิ์ 11 แท็บ**/เครื่อง POS/แจ้งเตือน

---

## ความถูกต้องของตัวเลข

`pnpm test` — **143 unit test** ครอบคลุมหัวข้อ 7 ทั้งหมด
`node scripts/acceptance.mjs` — **62 ข้อตรวจ** ตามหัวข้อ 12

ข้อมูล seed สร้างจากตัวเลขจริงของร้าน และระบบคำนวณกลับมาได้ตรงเป๊ะ:

| ตัวเลขของวันที่ 22/09/2026 (21 บิลแรก) | สเปก | ระบบ |
|---|---|---|
| ยอดขาย | 4,755.00 | ✅ 4,755.00 |
| กำไร | 1,625.02 | ✅ 1,625.02 |
| ส่วนลด | 305.00 | ✅ 305.00 |
| รวมก่อนลด | 5,060.00 | ✅ 5,060.00 |
| จำนวนบิล / เฉลี่ยต่อบิล | 20 / 237.75 | ✅ 20 / 237.75 |
| ยกเลิก | 1 บิล 195.00 | ✅ 1 บิล 195.00 |

รวมทั้งยอดรายกลุ่มสินค้าทั้ง 7 กลุ่ม, สินค้าขายดี 10 อันดับ, ยอดรายบิลทั้ง 24 ใบ
และยอดคงเหลือของสินค้าทุกตัวที่สเปกระบุไว้

ดูรายละเอียดสิ่งที่ทำครบและสิ่งที่ยังเหลือได้ที่ [STATUS.md](./STATUS.md)

---

## สิทธิ์การเข้าถึง

ตารางสิทธิ์ 11 แท็บ × 4 คอลัมน์ (แสดง / เพิ่ม-แก้ไข / ลบ / ส่งออกไฟล์)
ตรวจสิทธิ์ **ที่ API ทุก endpoint** ไม่ใช่แค่ซ่อนเมนูบน UI

บั๊กของระบบต้นฉบับที่แก้แล้ว: เปิด URL ที่มี `/` ต่อท้าย (เช่น `/product/`)
แล้วถูกล็อกทั้งที่เป็นเจ้าของร้าน — ของเรา normalize path (ตัด `/` ท้าย, ตัด query, lower-case)
ก่อนเทียบเสมอ และ router ก็มองว่าเป็น path เดียวกัน

---

## License

โค้ดนี้เขียนขึ้นสำหรับร้านมีดีทวีคูณ
