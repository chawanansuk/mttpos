# สถานะการพัฒนา — Medee POS

เทียบกับสเปก `POS-Clone-Spec.md` (เวอร์ชัน 1.0, 22 ก.ย. 2026)
อัปเดตล่าสุด: 22 กันยายน 2026

---

## สรุป

| ส่วน | สถานะ |
|---|---|
| สูตรคำนวณ (หัวข้อ 7) | ✅ ครบ · 143 unit test ผ่าน |
| Data model (หัวข้อ 4) | ✅ ครบทุกตาราง |
| Seed ข้อมูลจริง (หัวข้อ 10) | ✅ ครบ · ตัวเลขตรงกับระบบเดิม |
| API (หัวข้อ 9) | ✅ ครบส่วนที่อยู่ใน scope |
| หน้าขาย POS (หัวข้อ 5) | ✅ ฟังก์ชันหลักครบ · โต๊ะ/ครัว ยังไม่ทำ |
| หลังบ้าน (หัวข้อ 6) | ✅ 47 หน้า |
| Acceptance (หัวข้อ 12) | ✅ 62/62 ข้อผ่าน |

---

## ✅ ทำครบแล้ว

### สูตรคำนวณ (หัวข้อ 7) — `packages/domain`
- **7.1 โครงสร้างยอดในบิล** — ยอดรายบรรทัด, มูลค่าตัวเลือก, ส่วนลด 2 ชั้น,
  การกระจายส่วนลดท้ายบิลตามสัดส่วน (ปัด 2 ตำแหน่ง เศษไปแถวสุดท้าย),
  ค่าบริการ, VAT รวม/แยก, ปัดเศษ, ค่าจัดส่ง, ต้นทุน, กำไร
- **7.2 ส่วนลด** — รายการ (บาท/%) + ท้ายบิล, ห้ามทำให้ยอดติดลบ, เพดาน % ต่อพนักงาน
- **7.3 VAT** — ระดับร้าน + ระดับสินค้า (V/N), คอลัมน์รายงานทุกตัวออกจากสูตรเดียวกัน
- **7.4 ราคาหลายระดับ** — step price, ราคาช่องทาง (ขายส่งสมาชิก/เดลิเวอรี่), GP, ราคาด่วน
- **7.5 สต็อก** — ratio ของ PLU, อนุญาตติดลบ, ต้นทุนเฉลี่ยถ่วงน้ำหนัก, สถานะสต็อก,
  มูลค่าสต็อกรวม, ผลต่างจากเอกสารตรวจนับ, BOM, Serial
- **7.6 วันขาย** — ตัดตามเวลาเปิด-ปิดสาขา รองรับร้านที่ปิดหลังเที่ยงคืน
- **7.7 ยกเลิกบิล** — คืนสต็อก/คะแนน, บันทึกผู้ยกเลิก, เลขบิลไม่นำกลับมาใช้
- **7.8 คะแนนสมาชิก** — floor(ยอด ÷ บาทต่อคะแนน), หักคืนเมื่อยกเลิก
- **7.9 เลขเอกสารรัน** — ครบทุกประเภท, ออกแบบ atomic ที่ชั้น DB
- **7.10 ซิงค์/ออฟไลน์** — idempotency key, master data delta sync
- **2.3 ตารางสิทธิ์** — 11 แท็บ × 4 คอลัมน์ พร้อม normalizePath แก้บั๊กต้นฉบับ
- PromptPay QR (EMVCo) พร้อม CRC-16/CCITT

### หลังบ้าน (หัวข้อ 6) — 47 หน้า
โครงหน้าจอตาม 6.0 · รายงานสรุป (6.2) · ประวัติการขาย (6.3) · บิลที่เปิดอยู่ (6.4) ·
รายงาน 25 หน้า (6.6–6.8) ส่งออก xlsx/csv ได้ทุกหน้า · จัดการสินค้าครบ (6.9) ·
เอกสารคลัง + โอนระหว่างสาขา (6.10) · ร้านค้าและสาขา (6.11) · สมาชิก (6.12) ·
ตั้งค่าทั้งหมด (6.13) รวมตารางสิทธิ์และเครื่อง POS

### หน้าขาย (หัวข้อ 5)
5.1 ตั้งค่าเครื่อง + PIN · 5.2 เมนูหลัก · 5.3 หน้าขาย (ครบทุก sheet) ·
5.4 ชำระเงิน · 5.5 จัดการบิล · 5.6 รอบเงินสด · 5.7 สินค้าบนเครื่อง ·
5.9 สมาชิก · 5.13 ตั้งค่าบนเครื่อง · ออฟไลน์ + ซิงค์อัตโนมัติ

---

## ⚠️ ทำบางส่วน

| หัวข้อ | สถานะ |
|---|---|
| **5.8 คลังสินค้าบนเครื่อง** | ดูสินค้าและคงเหลือได้ · เพิ่มสินค้าแบบเร็วได้ · **ยังไม่มี** ฟอร์มรับเข้า/จ่ายออก/ปรับปรุง/ตรวจนับบนเครื่อง (ทำได้จากหลังบ้าน) |
| **5.10 โต๊ะ** | schema + API (เปิด/ย้าย/เคลียร์โต๊ะ) พร้อมแล้ว · **ยังไม่มี** หน้าจอแผนผังโต๊ะ แยกบิล รวมโต๊ะ |
| **8. การพิมพ์** | ใบเสร็จ 58/80mm ผ่านเบราว์เซอร์ (window.print) ครบตามรูปแบบหัวข้อ 8.1 · **ยังไม่มี** ESC/POS ผ่าน print-agent, ใบกำกับภาษีเต็มรูป A4 PDF, ใบออเดอร์ครัว, สติ๊กเกอร์บาร์โค้ด |
| **6.9.8 จัดการแปลภาษา** | เก็บ `translations` ใน DB และ API รับค่าได้ · **ยังไม่มี** หน้าจอแปล |
| **การแจ้งเตือน** | ตั้งค่าและเก็บค่าได้ · **ยังไม่ส่งจริง** ไปยัง LINE/Telegram |
| **i18n TH/EN** | ข้อความทั้งหมดเป็นภาษาไทยตามสเปก · **ยังไม่มี** ปุ่มสลับภาษาและไฟล์ EN |

---

## ⛔ ยังไม่ได้ทำ (อยู่นอก scope เวอร์ชันแรกตามหัวข้อ 1.2)

เตรียม schema และ feature flag ไว้แล้ว แต่ยังไม่มีหน้าจอ/การเชื่อมต่อจริง:

- เชื่อม Grab / Lineman (หน้า `/admin/grabdashboard` แสดงหน้าโปรโมท)
- โปรโมชัน (ตาราง `promotions` + `promotion_usages` พร้อมแล้ว, หน้าโปรโมท)
- QR Order ให้ลูกค้าสั่งเอง · Kiosk · จอคิว · จอแสดงผลลูกค้า
- E-Menu / CRM LINE OA · บุฟเฟต์/แพ็กเกจ
- เครื่อง Order (Staff Order device) — มีรายงานรองรับแล้ว แต่ยังไม่มีแอป
- ใบกำกับภาษีเต็มรูป A4 — API ออกเลข `IV2609-0001` ได้แล้ว แต่ยังไม่มี PDF

---

## หมายเหตุเรื่องข้อมูล seed

สเปกเปิดเผยตัวเลขสรุปของร้าน แต่ไม่ได้เปิดเผยรายการสินค้ารายบิลของทั้งเดือน
ข้อมูล seed จึงแบ่งเป็น 2 ส่วน:

**ข้อมูลจริงทั้งหมด** — ตั้งค่าร้าน, เครื่อง POS, พนักงาน, กลุ่มสินค้า 16 กลุ่ม,
หน่วยบรรจุ 7 หน่วย, ตัวเลือก 3 รายการ, สินค้า ~145 รายการพร้อมราคา/ต้นทุน/คงเหลือ,
สมาชิก, รายการนำเงินเข้า-ออก 12 รายการ, เอกสาร RCV0000756,
และ **บิลทั้ง 24 ใบของวันที่ 22/09/2026** (คำนวณกลับมาได้ตรงทุกตัวเลขที่สเปกระบุ)

**ข้อมูลที่สร้างขึ้น** — บิลย้อนหลัง 23/08–21/09/2026 สร้างจากแคตตาล็อกจริง
โดยบังคับให้ยอดรวมตรงกับที่ระบบเดิมรายงาน:

| ตัวเลขช่วง 23/08–22/09 | ระบบเดิม | seed ของเรา |
|---|---|---|
| ยอดขายรวม | 123,599.00 | ✅ 123,599.00 |
| จำนวนบิล | 773 | ✅ 773 |
| เฉลี่ยต่อบิล | 159.90 | ✅ 159.90 |
| ยกเลิก | 11 บิล / 2,560.00 | ✅ 11 บิล / 2,560.00 |
| ส่วนลดรวม | 7,806.00 | ~7,846.00 (คลาดเคลื่อน 0.5% จากการปัดยอดบิลเป็นหลัก 5 บาท) |
| กำไรรวม | 54,153.76 | ~56,267 (ต้นทุนของบิลที่สร้างขึ้นมาจากแคตตาล็อกของเรา ไม่ใช่รายการจริง) |

ยอดของวันที่ 22/09/2026 ซึ่งเป็นเป้าหมายใน acceptance checklist **ตรงทุกตัว** เพราะ seed จากรายการจริง

สเปกเองก็มีจุดที่ตัวเลขไม่สอดคล้องกัน เช่น กำไรทั้งวันของ 22/09 (2,031.02)
สูงกว่ากำไรของ 21 บิลแรก (1,625.02) อยู่ 406 บาท ทั้งที่ยอดขายเพิ่มขึ้นเพียง 400 บาท
ระบบจึงยึดตัวเลขชุด 21 บิลแรกที่สเปกกำหนดเป็นเกณฑ์ยอมรับ (หัวข้อ 12 ข้อ 13)

---

## ข้อควรทำก่อนใช้งานจริง

1. **เปลี่ยนรหัสผ่านและ PIN ทั้งหมด** — ค่าที่ seed ไว้เป็นค่าสำหรับทดลองเท่านั้น
2. **ตั้ง `JWT_SECRET` / `JWT_REFRESH_SECRET`** เป็นสตริงสุ่มยาว ๆ
3. **กรอกเลขพร้อมเพย์จริง** ที่ ตั้งค่า → การชำระเงิน (seed ใช้เบอร์ร้านเป็นค่าตั้งต้น)
4. **นำเข้าสินค้าที่เหลือ** — ระบบเดิมมี 1,341 SKU · seed มี ~145 รายการที่สเปกบันทึกไว้
   ที่เหลือ export เป็น CSV จากระบบเดิมแล้วนำเข้าที่ สินค้า → นำเข้ารายการสินค้า
5. **ตั้ง HTTPS และสำรองฐานข้อมูลรายวัน**

---

## Deploy บน Vercel

ต่อมาเพิ่มความสามารถให้ deploy แบบ serverless ได้จากโค้ดชุดเดียวกัน สิ่งที่เปลี่ยน:

| เรื่อง | เดิม | ตอนนี้ |
|---|---|---|
| แฮชรหัสผ่าน | `argon2` (native addon — คอมไพล์ลง serverless ไม่ได้) | `scrypt` ของ `node:crypto` ใน `@medee/db/password` (มี unit test 6 เทสต์) |
| ตัวแอป Fastify | สร้างและ `listen()` ในไฟล์เดียว | แยก `buildApp()` ออกมาที่ `apps/api/src/app.ts` — `index.ts` เหลือแค่ตัว `listen()` |
| ทางเข้า API | เว็บ rewrite `/api/v1/*` ไปพอร์ต 4000 เสมอ | ถ้าไม่ตั้ง `API_URL` เว็บจะเสิร์ฟเองที่ `apps/web/src/app/api/v1/[[...path]]/route.ts` ผ่าน `app.inject()` |
| เรียลไทม์ | ต่อ `/ws` ใหม่ทุก 5 วินาทีไม่จำกัด | ถ้าต่อไม่ติด 3 ครั้งแรกจะเลิกพยายาม แล้วเปลี่ยนไปดึงข้อมูลซ้ำตามรอบแทน |

ตรวจแล้วทั้งสองเส้นทาง: acceptance checklist **62/62 ผ่านทั้งแบบสองโปรเซสและแบบโปรเซสเดียว**
รวมถึงการดาวน์โหลด `.xlsx` ซึ่งเป็น binary (ผ่าน route handler แล้วได้ไฟล์ Excel ที่เปิดได้จริง)

**ยังไม่เท่าเดิมในโหมด serverless**

- **ไม่มี WebSocket** — หน้ารายงานสรุปและบิลที่เปิดอยู่จะช้ากว่าเดิม 15–30 วินาที
  ถ้าต้องการเรียลไทม์จริงให้ deploy `apps/api` แยกบนเครื่องที่รันค้างได้ (Docker/VPS) แล้วตั้ง `API_URL`
- **rate limit ไม่ทำงาน** — ตัวนับอยู่ในหน่วยความจำของแต่ละอินสแตนซ์
  ข้อกำหนด "ใส่ PIN ผิดได้ 5 ครั้ง/นาที" (หัวข้อ 11) จึงบังคับไม่ได้บน Vercel
  (การล็อกบัญชีหลังใส่ PIN ผิดสะสมยังทำงานปกติ เพราะนับในฐานข้อมูล)
- **ต้องใช้ connection pooler** — ถ้าชี้ `DATABASE_URL` ไปที่ Postgres ตรง ๆ connection จะเต็มเมื่อมีคนใช้พร้อมกันหลายคน


---

## สถานะการ deploy

| | |
|---|---|
| เว็บ | https://medee-pos.vercel.app (Vercel · ภูมิภาค sin1) |
| ฐานข้อมูล | Supabase Postgres · ap-southeast-1 (Singapore) |
| ตอนรัน | `DATABASE_URL` ผ่าน transaction pooler พอร์ต 6543 (`pgbouncer=true&connection_limit=1`) |
| ตอน build | `DIRECT_DATABASE_URL` ผ่าน session pooler พอร์ต 5432 |

**ตรวจแล้วบนลิงก์จริง: acceptance 62/62 ผ่าน** — รวมเข้าสู่ระบบ, รายงานครบ 25 หน้า,
ดาวน์โหลด .xlsx, สร้างบิลจริงแล้วยกเลิก, ตัดและคืนสต็อก, กันบิลซ้ำด้วย clientId และ PromptPay QR

ปัญหาที่เจอตอน deploy จริงและวิธีแก้ (บันทึกไว้กันลืม):

1. **seed ใช้เวลาเกิน 30 นาทีจนต้องยกเลิก build** — build machine อยู่ US East แต่ฐานข้อมูลอยู่ Singapore
   การเขียนบิลทีละใบกลายเป็นราว 5,000 รอบข้ามทวีป แก้ด้วย `createSeedReceiptsBatch()`
   ที่เขียนด้วย `createMany` เหลือไม่กี่สิบรอบ — ประวัติ 760 บิลลดจาก 30+ นาที เหลือ **45 วินาที**
2. **API ตอบ 500 ทุกครั้งที่แตะฐานข้อมูล** — `Prisma Client could not locate the Query Engine
   for runtime rhel-openssl-3.0.x` เพราะ query engine เป็นไฟล์ `.node` ที่ bundler มองไม่เห็น
   และ pnpm วางไว้ใต้ `.pnpm/` ที่ trace ตามลิงก์ไปไม่ถึง แก้ด้วย `outputFileTracingRoot`
   และ `outputFileTracingIncludes` ใน next.config

**สิ่งที่ต้องระวัง**

- `SEED_DATABASE` บน Vercel ตั้งเป็น `off` อยู่ ถ้าเปลี่ยนเป็น `yes-wipe-and-seed`
  แล้ว deploy ใหม่ **จะล้างข้อมูลทั้งหมดทิ้งแล้ว seed ใหม่**
- การรัน acceptance ทิ้งบิลทดสอบที่ถูกยกเลิกแล้วไว้ 1 ใบ (เครื่อง 001) ตามวันที่ที่รัน
  ไม่กระทบตัวเลขของ 22/09/2026 ซึ่งเป็นเกณฑ์ยอมรับ
